name: Deploy GraphForms Backend to AWS Elastic Beanstalk

on:
  push:
    branches:
      - main

env:
  AWS_REGION: us-east-1 

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1
      
    - name: Initialize Go module
      run: go mod init github.com/monccciii/backend
      
    - name: Build Go application
      run: |
        cd backend
        go build -o bin/application
        
    - name: Create S3 bucket if it doesn't exist
      run: aws s3api create-bucket --bucket Graphforms --region us-east-1 --create-bucket-configuration LocationConstraint=us-east-1 || true

    - name: Package and upload to S3
      run: |
        cd backend
        zip -r Graphforms-Backend-${{ env.GITHUB_SHA }}.zip bin/application
        aws s3 cp Graphforms-Backend-${{ env.GITHUB_SHA }}.zip s3://Graphforms/Graphforms-Backend-${{ env.GITHUB_SHA }}.zip


    - name: Deploy to AWS Elastic Beanstalk
      run: |
        aws elasticbeanstalk create-application-version \
          --application-name Graphforms \
          --version-label Graphforms-Backend-${{ env.GITHUB_SHA }} \
          --source-bundle S3Bucket=Graphforms,S3Key=Graphforms-Backend-${{ env.GITHUB_SHA }}.zip

        aws elasticbeanstalk update-environment \
          --environment-name Graphforms-Prod \
          --version-label Graphforms-Backend-${{ env.GITHUB_SHA }}
